---
import { cargarProductos } from "../../utils/loadCSV";
import Layout from "../../layouts/Layout.astro";
import { config } from "../../config";
import NumberInput from "@components/NumberInput.astro";

export function getStaticPaths() {
  const productos = cargarProductos();

  return productos.map((p) => {
    const { id, relacionados, ...rest } = p;

    const productosRelacionados = productos.filter((p) =>
      relacionados.includes(p.id),
    );

    return {
      params: { id },
      props: { id, productosRelacionados, ...rest },
    };
  });
}

const {
  id,
  name,
  img,
  price,
  categories,
  paymentLink,
  description,
  productosRelacionados: relacionados,
} = Astro.props;
---

<Layout
  pageTitle={`${config.pageTitle} - ${name}`}
  description={description}
  pageImage={img[0]}
>
  <section class="productContainer">
    <!-- BREADCRUMB -->
    <div class="breadcrumb">
      <a href=`${import.meta.env.BASE_URL}/categories/${categories.name}`
        >{categories.name}</a
      >

      {categories.subcategories.length >= 1 && "/"}
      {
        categories.subcategories.map((item, i) => (
          <>
            <a
              href={`${import.meta.env.BASE_URL}/categories/${categories.name}/${item.name}`}
            >
              {item.name}
            </a>
            {i < categories.subcategories.length - 1 && " / "}
          </>
        ))
      }
    </div>

    <div class="product_layout">
      <!-- LEFT IMAGE SECTION -->
      <div class="gallery">
        <img class="mainImg" src={img[0]} alt={name} />

        <div class="thumbs">
          {img.map((i) => <img src={i} alt="" class="thumb" />)}
        </div>
      </div>

      <!-- RIGHT INFO -->
      <div class="infoBox">
        <span class="stock">En Stock</span>
        <h1 class="name">{name}</h1>

        <div class="meta">
          <p><strong>ID:</strong> {id}</p>
          <p><strong>Marca:</strong> {categories}</p>
        </div>
        <strong>Descripción del Producto:</strong>
        <p class="description">{description}</p>
        <strong>Talle:</strong>
        <div class="sizes">
          <button class="size">S</button>
          <button class="size">M</button>
          <button class="size">L</button>
          <button class="size">XL</button>
        </div>

        <div class="priceRow">
          <span class="price">${price}</span>
          <NumberInput
            id="cartQty"
            name="pasajeros"
            initialValue={2}
            min={1}
            max={10}
          />
          <button
            type="button"
            id="addToCartBtn"
            class="buyBtn"
            data-id={Astro.props.id}
          >
            Añadir al Carrito
          </button>
        </div>
      </div>
    </div>

    <!-- RELATED PRODUCTS -->

    {
      relacionados && relacionados.length > 0 && (
        <div class="related">
          <h2>Productos Relacionados:</h2>

          <div class="relatedList">
            {relacionados.map((prod) => (
              <div class="card">
                <a href={`${import.meta.env.BASE_URL}/producto/${prod.id}`}>
                  <div class="cardImg">
                    <img class="cardImg" src={prod.img[0]} alt={prod.name} />
                  </div>
                  <span class="cardTitle">{prod.name}</span>
                  <span class="cardPrice">${prod.price}</span>
                </a>
              </div>
            ))}
          </div>
        </div>
      )
    }
  </section>
</Layout>

<style>
  .productContainer {
    max-width: 1200px;
    min-height: 100vh;
    margin: 0 auto;
    padding: 20px;
    font-family: "Poppins", sans-serif;
    margin-bottom: 3rem;
  }

  /**** Breadcrumb *****/
  .breadcrumb {
    font-size: 14px;
    font-weight: 700;
    color: #000;
    margin-bottom: 20px;
    padding-bottom: 10px;
    border-bottom: 1px solid #000;
  }
  .breadcrumb a {
    text-decoration: none;
    color: inherit;
  }
  .breadcrumb span {
    color: #000;
  }

  /**** Layout *****/
  .product_layout {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }

  /**** Gallery *****/
  .gallery {
    width: 100%;
  }

  .mainImg {
    width: 100%;
    height: auto;
    aspect-ratio: 1/1;
    object-fit: cover;
    background: #000; /* Placeholder placeholder color from image */
    display: block;
    margin-bottom: 10px;
  }

  .thumbs {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    padding-bottom: 5px; /* For scrollbar if needed */
  }

  .thumb {
    width: 60px;
    height: 60px;
    border-radius: 0;
    object-fit: cover;
    cursor: pointer;
    border: 1px solid #ddd;
  }

  /**** Info box *****/
  .infoBox {
    padding: 0;
  }

  /* Badge style like "Por encargo" */
  .stock {
    display: inline-block;
    background: #5cb85c; /* Green color */
    color: white;
    font-weight: 700;
    font-size: 14px;
    padding: 4px 8px;
    text-transform: uppercase;
    margin-bottom: 10px;
    border-radius: 0;
  }

  .name {
    font-size: 28px;
    font-weight: 800;
    line-height: 1.2;
    margin: 0 0 15px 0;
    color: #000;
  }

  .meta {
    font-size: 14px;
    color: #000;
    margin-bottom: 15px;
    line-height: 1.6;
  }

  .meta p {
    margin: 2px 0;
  }

  .meta strong {
    font-weight: 800;
  }

  .description {
    font-size: 14px;
    line-height: 1.5;
    margin-bottom: 20px;
    color: #000;
  }

  strong {
    font-weight: 800;
  }

  /**** Sizes *****/
  .sizes {
    display: flex;
    gap: 5px;
    margin: 10px 0 20px 0;
  }

  .size {
    width: 40px;
    height: 40px;
    background: white;
    border: 1px solid #000;
    color: #000;
    font-size: 32px;
    font-weight: 250;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;
  }

  .size:hover {
    background: #f0f0f0;
  }

  /**** Price & Actions *****/
  .priceRow {
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    gap: 15px;
    margin-top: 20px;
  }

  .priceRow .price {
    display: block;
    height: 50px;
    line-height: 160%;
    font-size: 32px;
    font-weight: 800;
    color: #000;
    margin-right: auto;
  }

  /* Adjust NumberInput styling overlap if possible, otherwise wrapper */
  /* Assuming NumberInput generates a wrapper, we might need to target it deep or adjacent */

  .buyBtn {
    background: #e67e22; /* Orange/Terra-cotta */
    color: white;
    border: none;
    padding: 0 20px;
    height: 44px; /* Match typical touch target */
    /*font-size: 16px;*/
    font-weight: 700;
    cursor: pointer;
    border-radius: 4px;
    flex: 1;
    min-width: 140px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .buyBtn:hover {
    background: #d35400;
  }

  /**** Related Products *****/
  .related {
    margin-top: 50px;
    border-top: 1px solid #000;
    padding-top: 20px;
  }

  .related h2 {
    font-size: 24px;
    font-weight: 800;
    margin-bottom: 20px;
    text-align: center;
  }

  .relatedList {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
  }

  .card {
    background: white;
    text-align: center;
  }

  .card a {
    text-decoration: none;
    color: inherit;
    cursor: pointer;
  }

  .cardImg {
    width: 100%;
    aspect-ratio: 1/1;
    object-fit: cover;
    margin-bottom: 10px;
  }

  .cardTitle {
    font-size: 14px;
    font-weight: 700;
    display: block;
    margin-bottom: 5px;
  }

  .cardPrice {
    font-size: 16px;
    font-weight: 800;
    display: block;
    margin-bottom: 10px;
  }

  .cardBtn {
    background: transparent; /* Or keep black if desired, but reference focuses on main product */
    color: #000;
    text-decoration: underline;
    border: none;
    font-weight: 600;
    cursor: pointer;
    padding: 0;
  }

  /**** Desktop Media Query *****/
  @media (min-width: 768px) {
    .product_layout {
      flex-direction: row;
      align-items: flex-start;
    }

    .gallery {
      flex: 1;
      max-width: 50%;
    }

    .infoBox {
      flex: 1;
      padding-left: 20px;
    }

    .price {
      width: auto;
      margin-right: 20px;
    }

    .buyBtn {
      flex: initial;
      width: auto;
    }

    .relatedList {
      grid-template-columns: repeat(4, 1fr);
    }
  }
</style>

<script>
  import addToCart from "../../utils/addToCart";
  const addBtn = document.getElementById("addToCartBtn");

  addBtn?.addEventListener("click", () => {
    const qtyInput = document.getElementById("cartQty") as HTMLInputElement;
    const price = document.querySelector(".price") as HTMLSpanElement;
    const name = document.querySelector(".name") as HTMLHeadingElement;
    const id = addBtn?.getAttribute("data-id") ?? "";
    const img = document.querySelector(".thumbs img") as HTMLImageElement;

    const qty = parseInt(qtyInput.value, 10);

    addToCart(
      id,
      name.innerText,
      price.innerText.replace("$", "").replace(",", "."),
      qty,
      img.src,
    );

    // llama a una funcion declarada en header que actualiza el contador del carrito
    window.updateCartCount && window.updateCartCount();
    addBtn.textContent = "¡Agregado!";
    setTimeout(() => (addBtn.textContent = "Agregar al carrito"), 1200);
  });
</script>
