---
import Layout from "../layouts/Layout.astro";
import { config } from "../config";
import { Icon } from "astro-icon/components";
---

<Layout pageTitle={`${config.pageTitle} - Sobre nosotros`}>
  <section class="contact-container">
    <div class="contact-info">
      <h2>Contacto</h2>
      <hr />
      
      <p class="intro-text">
        Te invitamos a completar el formulario de contacto si tienes alguna duda, consulta o buscas algún Producto o Servicio particular, a la brevedad nos pondremos en contacto.
      </p>

      <div class="services-list">
        <strong>Comercializamos los siguientes servicios:</strong>
        <ul>
          <li>- De Codificación Biológica por 80 dólares</li>
          <li>- Carta Numerológica por 80 dólares.</li>
        </ul>
      </div>

      <p class="cta-text">
        No dudes en contactarnos para más detalles o para realizar tu consulta
      </p>

      <div class="contact-details">
        <div class="detail-item">
          <div class="icon-wrapper mobile">
            <Icon name="phone" width="24" height="24" />
          </div>
          <div class="detail-text">
            <span>Telefono: +598 91 361 706</span>
            <span>Mail: bibisventasyserviciosonline@gmail.com</span>
          </div>
        </div>

        <div class="detail-item">
            <div class="icon-wrapper pin">
             <Icon name="location" width="24" height="24" />
            </div>
            <div class="detail-text">
             <span>Dirección: Baltasar Brum 1154, 60000 Paysandú, Departamento de Paysandú, Uruguay</span>
            </div>
        </div>
      </div>
      
      <hr class="separator"/>

      <div class="social-links">
        <a href="https://facebook.com/bibicartanumerologica" target="_blank" rel="noopener noreferrer">
            <Icon name="facebook" width="32" height="32" class="social-icon facebook" />
        </a>
        <a href="https://instagram.com/bibisventasonline" target="_blank" rel="noopener noreferrer">
            <Icon name="instagram" width="32" height="32" class="social-icon instagram" />
        </a>
        <a href="https://wa.me/+59891361706" target="_blank" rel="noopener noreferrer">
            <Icon name="whatsapp" width="32" height="32" class="social-icon whatsapp" />
        </a>
      </div>

    </div>

    <div class="contact-form-wrapper">
      <form class="contact-form">
        <input type="text" placeholder="Nombre Completo" required />
        <input type="tel" placeholder="Numero de Teléfono" required />
        <input type="text" placeholder="Asunto" required />
        <textarea placeholder="Tu Mensaje" rows="8" required></textarea>
        <button type="submit">Enviar</button>
      </form>
    </div>
  </section>
</Layout>

<style>
  .contact-container {
    max-width: 1100px; /* var(--max-width-container) */
    margin: 3rem auto;
    padding: 0 20px;
    display: flex;
    gap: 4rem;
    font-family: 'Poppins', sans-serif;
    color: #232946;
  }

  /* Left Column */
  .contact-info {
    flex: 1;
  }

  h2 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  hr {
    border: 0;
    border-top: 1px solid #e0e0e0;
    margin-bottom: 2rem;
  }
  
  hr.separator{
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .intro-text {
    line-height: 1.6;
    margin-bottom: 2rem;
    font-size: 0.95rem;
  }

  .services-list {
    margin-bottom: 2rem;
    font-size: 0.95rem;
  }
  
  .services-list strong {
      display: block;
      margin-bottom: 0.2rem;
  }

  .services-list ul {
    list-style: none;
    padding: 0;
  }

  .cta-text {
      margin-bottom: 2rem;
      font-size: 0.95rem;
  }

  .contact-details {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .detail-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .icon-wrapper {
      min-width: 40px; /* Fixed width for icon container to align text */
      text-align: center;
      display: flex;
      justify-content: center;
      padding-top: 2px;
  }
  
  .icon-wrapper svg {
       color: #999; /* Example gray color */
       width: 32px;
       height: 32px;
       stroke-width: 1.5;
  }
  .icon-wrapper.mobile svg{
      /* Adjust specific icon styles if needed */
       height: 48px; /* Phone icon in design looks simpler/outline */
       width: 40px;
        opacity: 0.4;
  }
    .icon-wrapper.pin svg{
      /* Adjust specific icon styles if needed */
        height: 48px;
         width: 40px;
         opacity: 0.4;
  }


  .detail-text {
    display: flex;
    flex-direction: column;
    font-size: 0.95rem;
    line-height: 1.4;
  }

  .social-links {
    display: flex;
    gap: 1rem;
  }

  .social-icon {
      /* Base styles handled by global styles or reset here if needed */
      color: #3b5998; /* Default for facebook-ish */
  }
    .social-icon.facebook { color: #3b5998; }
    .social-icon.instagram { color: #8a3ab9; /* approx gradient fallback */ }
    .social-icon.whatsapp { color: #25d366; }

  /* Right Column - Form */
  .contact-form-wrapper {
    flex: 1;
    padding-top: 3rem; /* Align with text start approximately */
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  input, textarea {
    width: 100%;
    padding: 12px 15px;
    border: 1px solid #777;
    border-radius: 25px; /* Pill shape */
    font-size: 1rem;
    color: #555;
    background: transparent;
    outline: none;
    font-family: inherit;
  }
  
  textarea {
      border-radius: 20px; /* Less rounded for textarea usually, but image shows rounded corners */
      resize: vertical;
  }

  input::placeholder, textarea::placeholder {
    color: #888;
  }

  button {
    background-color: #e86c1a; /* Orange from design approx */
    color: white;
    border: none;
    padding: 10px 30px;
    border-radius: 25px;
    font-weight: 700;
    font-size: 1.1rem;
    cursor: pointer;
    align-self: flex-start;
    transition: background 0.3s;
  }

  button:hover {
    background-color: #d65a0f;
  }

  @media (max-width: 768px) {
    .contact-container {
      flex-direction: column;
      gap: 2rem;
    }
    
    .contact-form-wrapper {
        padding-top: 0;
    }
  }
</style>
